<UserControl x:Class="MicroserviceManager.Views.ProcesseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <DockPanel>
        <Label x:Name="label" Content="List of Processes :"  VerticalAlignment="Top" DockPanel.Dock="Top"/>
        <DataGrid x:Name="dataGrid" AutoGenerateColumns="False" DockPanel.Dock="Bottom" >
            <DataGrid.Columns>
                <DataGridTextColumn Header="Process Name" Binding="{Binding Name}" Width="1*" >
                </DataGridTextColumn>
                <DataGridTextColumn Header="User Name" Binding="{Binding UserName}" Width="2*">
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</UserControl>
